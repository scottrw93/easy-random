buildpack:
  name: Blazar-Buildpack-Java

enabled: true

provides:
  - name: easy-random-core
    version: 5.0.1-$GIT_BRANCH-SNAPSHOT

env:
  SET_VERSION_OVERRIDE: 5.0.1-$GIT_BRANCH-SNAPSHOT

depends:
  - easy-random-tests

