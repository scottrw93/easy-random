enabled: true

provides:
  - name: easy-random-tests
    version: 5.0.1-$GIT_BRANCH-SNAPSHOT

env:
  SET_VERSION_OVERRIDE: 5.0.1-$GIT_BRANCH-SNAPSHOT

buildpack:
  host: git.hubteam.com
  organization: HubSpotProtected
  repository: Blazar-Buildpack-Java
  branch: v2
